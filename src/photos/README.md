Drop your image files (jpg, jpeg, png, webp, gif) directly in this folder.

They are picked up automatically by Vite's `import.meta.glob` in `src/App.jsx` — no manual registration needed. Just add files here and refresh (or let HMR pick them up in dev).
